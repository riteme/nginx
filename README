
Documentation is available at http://nginx.org

## Compile & Run on Gramine

Requires [ModSecurity-nginx](https://github.com/SpiderLabs/ModSecurity-nginx).

```
./configure --prefix=$(abspath ./local) --add-module=../modsecurity-nginx --without-http_rewrite_module --with-http_ssl_module
make
make install
```
